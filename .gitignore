# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.db
backend/deepscroll.db.legacy_*
.env

# Node / Next.js
node_modules/
.next/
.env.local

# Performance audit artifacts
.perf/

# Verification artifacts (screenshots, scratch scripts, export checks)
.verify/

# Logs
*.log
.claude/skills/ui-ux-pro-max

# OS / Editor
.DS_Store
.vscode/
!.vscode/extensions.json
.idea/

docs/content-structure/generated/*/_batches/

# User-uploaded content (runtime data, not source)
user_uploads/

# Thumbnail generator caches: downloaded Natural Earth basemap, cached OSM
# geometry, fallback font. All re-fetchable, none of it source.
backend/data/
# The CLI's default --out-dir, at the repo root only. A bare "thumbnails/"
# would also swallow the backend/app/thumbnails/ source package.
/thumbnails/